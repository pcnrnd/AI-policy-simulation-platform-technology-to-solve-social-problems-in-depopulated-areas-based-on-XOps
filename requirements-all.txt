# =============================================================================
# Population Decline — 통합 의존성 (루트 + app/ 전체)
# =============================================================================
# 본 파일은 다음을 단일 파일로 통합한 버전입니다:
#
#   • 루트 requirements.txt          (Streamlit 데이터 탐색 데모)
#   • app/requirements.txt           (P0/P1/P2 12개 모듈 runtime)
#   • app/requirements-dev.txt       (테스트·린트·타입체크)
#   • app/requirements-ml.txt        (SHAP·XGBoost·LightGBM)
#
# 설치(전부):
#   python -m pip install -r requirements-all.txt
#
# 부분 설치는 원본 파일을 사용하세요:
#   python -m pip install -r requirements.txt                  # streamlit 데모만
#   python -m pip install -r app/requirements.txt              # app runtime만
#   python -m pip install -r app/requirements-dev.txt          # app + dev
#   python -m pip install -r app/requirements-ml.txt           # app + ML
#
# Python: >= 3.11 권장 (app 모듈 요구사항).
# 본 통합본 사용 시 디스크 ~1.5GB, 첫 설치 5-10분.
# -----------------------------------------------------------------------------


# =============================================================================
# [1] Streamlit 데이터 탐색 데모 (루트)
# =============================================================================

# --- Web UI -------------------------------------------------------------------
streamlit==1.36.0

# --- Data ---------------------------------------------------------------------
pandas==2.2.2
duckdb==1.0.0

# --- Visualization ------------------------------------------------------------
plotly==5.22.0


# =============================================================================
# [2] app/ Runtime — P0/P1/P2 12개 모듈
# =============================================================================

# --- Web Framework ------------------------------------------------------------
fastapi>=0.115
uvicorn[standard]>=0.30

# --- Validation / Settings ----------------------------------------------------
pydantic>=2.7
pydantic-settings>=2.4
python-dotenv>=1.0

# --- HTTP Client (Alerter 다채널 알림) ---------------------------------------
httpx>=0.27

# --- Numerics / ML 기본 -------------------------------------------------------
numpy>=1.26
scikit-learn>=1.5

# --- Observability ------------------------------------------------------------
prometheus-client>=0.20


# =============================================================================
# [3] 개발 의존성 — 테스트·린트·타입체크
# =============================================================================

# --- Test ---------------------------------------------------------------------
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0

# --- Lint / Type --------------------------------------------------------------
ruff>=0.6
mypy>=1.10


# =============================================================================
# [4] 선택 ML — SHAP·XGBoost·LightGBM
# =============================================================================
# 미설치 시 본 모듈은 분산/순열 폴백으로 안전 동작.

# --- Explainability -----------------------------------------------------------
shap>=0.46

# --- Boosted Trees ------------------------------------------------------------
xgboost>=2.0
lightgbm>=4.3
