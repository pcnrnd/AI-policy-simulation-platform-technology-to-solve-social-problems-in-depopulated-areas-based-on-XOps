# 1. 지도 기능(PostGIS)이 포함된 베이스 이미지를 가져옵니다.
FROM postgis/postgis:16-3.4

# 2. 벡터 기능(pgvector)을 추가로 설치합니다.
RUN apt-get update \
    && apt-get install -y postgresql-16-pgvector \
    && rm -rf /var/lib/apt/lists/*