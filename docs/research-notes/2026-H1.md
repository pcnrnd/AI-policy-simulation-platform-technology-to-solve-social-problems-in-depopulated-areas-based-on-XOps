# 2026 상반기 — 대시보드 SPA 프로토타입 (decline_poc)

> 기록일 2026-09-01 (소급 재구성). 이 반기는 커밋 이력과 `decline_poc/CLAUDE.md`
> (프론트 이식 후 `platform/frontend/CLAUDE.md`로 계승)가 근거다.

## 2026-06-08 — vanilla 프로토타입 → React 18 + Vite 리팩토링

- **수행 기간**: 2026-06-08 / 기록일: 2026-09-01
- **목표**: 인구감소 R&D 3차년도 데모 대시보드의 유지보수 가능한 컴포넌트 구조 확보
- **수행 내용**: Zero-dependency vanilla JS/CSS(단일 `app.js`) 구현을 React 18 + Vite로
  전환. 사이드바 탭 셸(App.jsx) + 페이지 6종(Overview·Monitor·Orchestrator·DataOps·
  Simulator·Reporter) + 전역 상태 `AppStateContext` 구조를 확립. mock 시드는
  `mock_data.json` 단일 파일로 통합(지역·지표 시계열·드리프트 분포·SHAP·카탈로그)
- **의사결정**: 추가 번들러·테스트 러너·CSS 전처리기 미도입(단순성 유지),
  상태는 Context API 단일 출처 — R&D 데모 규모에서 상태 라이브러리는 과설계로 판단
- **근거**: 커밋 `9259ab8`~`27d7582`(2026-06-08 일괄), `9115083`(mock_data.json 시드),
  `decline_poc/CLAUDE.md` v3.1.0 개정 이력

## 2026-06-10~12 — 도메인 화면·라이브러리 계층 구축

- **수행 기간**: 2026-06-10 ~ 06-12 / 기록일: 2026-09-01
- **수행 내용**:
  - `lib/` 무의존성 유틸 계층: `dataopsApi.js`(CRUD/JWT/OAuth2/SQL·MQL 생성 시뮬레이션 —
    이후 xops-service 백엔드 포팅의 원형), `simulation.js`(인구 예측·예산 계수),
    `zip.js`+`reportExport.js`(store-only ZIP으로 DOCX/XLSX/HWP 생성 — 외부 문서 라이브러리 없이
    OOXML 컨테이너 직접 작성), `geo.js`(격자 폴리곤·인구밀도 색상), `perf.js`(응답속도 계측)
  - 정책 추천 RICE 산정(`policyStrategies.js`) + Top3 카드, 시나리오 저장·비교
    (`ScenarioCompare.jsx`, localStorage), 다크/라이트 테마(`ThemeContext`)
  - DataOps 화면의 아카이브 등록 폼(`ArchiveRegisterForm.jsx`) — 메타데이터(스키마·티어·범위)
    정의 즉시 가상화 API 대상이 되는 흐름의 UI 원형
- **의사결정**: 지도는 Leaflet 직접 통합(`useEffect` 라이프사이클), 차트는 Chart.js —
  React 래퍼 외 추가 시각화 프레임워크 미도입
- **근거**: 커밋 `10f34c9`~`970df57`(06-10~12 일괄), `c5d4a9e`·`507d45a`·`941a872`
  (mock 시드 증분), `decline_poc/src/lib/` 파일 구성

## 반기 종합 — 기술 상태

| 축 | 상태 |
|---|---|
| 화면 | 6페이지 SPA(React 18 + Vite), 다크/라이트 테마, 접근성 기초(포커스 링·sr-only) |
| 데이터 | mock_data.json 단일 시드 — 백엔드 없이 전 화면 동작 |
| DataOps | SQL/MQL 생성·JWT·CRUD가 클라이언트 시뮬레이션(`dataopsApi.js`)으로 존재 |
| 다음 | 백엔드 실물화 — 클라이언트 시뮬레이션 로직의 서버 포팅(→ 2026-H2 xops-service) |
